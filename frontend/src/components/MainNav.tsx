import React from 'react'
import { Button } from './ui/button'

const MainNav = () => {
  return (
    <Button variant="ghost" className=' text-xl font-bold hover:text-blue-500 hover:bg-white' >
        Log In
    </Button>
  )
}

export default MainNav
